server:
  port: 8070

spring:
  application.name: eureka-server

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false

management:
  endpoints:
    web:
      exposure:
        include: "*"